pipeline {
    stages {
        stage('test node version') {
            steps {
                echo '-- Testing node version --'
                sh 'node --version'
                sh 'which node'
                // npm install
                // npm test
            }
        }
    }
}